import { Heading, Source, Typeset } from '@storybook/addon-docs/blocks'
import { Fragment } from 'react'

import fonts from '~styles/fonts.module'

export const sizes = ['1em', '2em', '3em', '4em']

# Fonts

`import fonts from '~styles/fonts.module'`

<br />

<div>
  {Object.keys(fonts).map(font => (
    <Fragment key={font}>
      <Heading>fonts.${font}</Heading>
      <Source code={`font-family: ${fonts[font]}`} language="css" />
      <Typeset fontFamily={fonts[font]} fontSizes={sizes} />
    </Fragment>
  ))}
</div>
